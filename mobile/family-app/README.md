# AI Glasses – Family App (Android)

App cho **người thân**, dùng để:

- Theo dõi vị trí hiện tại/historic của kính (qua cloud-backend).
- Nhận cảnh báo nguy hiểm, SOS.
- Xem trạng thái hoạt động.

Kết nối cloud qua:

- REST API (lấy lịch sử).
- WebSocket `/ws/family` (nhận realtime).
